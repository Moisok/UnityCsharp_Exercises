using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoisesEjercicio3 : MonoBehaviour
{

    public int valor1;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
 
        int suma = 12 + valor1;
        
        Debug.Log("12 " + " + " + valor1 + " = " + suma);

    }
}
